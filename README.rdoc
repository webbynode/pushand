= phd

1. deploy app to app server (prototype)
2. deploy app to app server automagically via git
3. remotely
4. ?

== first thing to make work:

  $ phd_server_setup
  $ git remote add local localhost:repo.git
  $ git push local master

  # check to see that it was deployed to localhost!
